% load packages
AXIS = lib.ecma.require(@lib.model.cRAR.axes.astro);

% set axes data
AX = {...
	'XLim',		[1E-5,1E4],...
	'XScale',	'log' ...
};

AY = {...
	'YLim',		[1E1,1E15],...
	'YScale',	'log' ...
};

% create axes
ax = lib.figure.axes(...
	'x',	AXIS.radius,...
	'y',	AXIS.density,...
	'axes',	[AX,AY] ...
);

% create figure
EXPORT = lib.figure.grid(ax);